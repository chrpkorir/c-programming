/**
 * Some facts about function pointers
 * 1. A function pointer points to code, not data
 * 2. We do not allocate de-allocate memory using function pointers.
 * 3. A functions name can also be used to get 
 * function's address.
 * 4. We can have an array pointers
 * 5. A function pointer can be passed as an argument 
 * and can also be returned from a function
 *
 * 6. Function pointer can be used in place of switch case
 */ 
